@model IEnumerable<BucketList.Models.MuseumType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MuseumsType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MuseumsType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MuseumTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MuseumTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MuseumTypeId })
        </td>
    </tr>
}

</table>
