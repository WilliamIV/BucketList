@model IEnumerable<BucketList.Models.ListCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ListCategories)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ListCategories)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ListCategoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ListCategoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ListCategoryId })
        </td>
    </tr>
}

</table>
