@model IEnumerable<BucketList.Models.RestraurantType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RestraurantsType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RestraurantsType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RestraurantTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RestraurantTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RestraurantTypeId })
        </td>
    </tr>
}

</table>
